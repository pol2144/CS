package Sprites;

import javafx.scene.image.ImageView;

public class Sheep extends Sprite{
    public Sheep( int row, int column, int hp, ImageView imageView, int width, int heigth) {
        super(row, column,hp, width, heigth);
    }

}
